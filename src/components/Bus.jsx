import React, { useState } from "react";

const styles = {
};

const Bus = () => {
    return (
        <div id="Bus" className="flex h-full rounded-lg px-4 py-1 border border-light-grey text-center items-center font-monospace font-bold text-lg">
            Bus
        </div>
    )
}

export default Bus;